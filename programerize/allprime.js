const low = parseInt(prompt("low"));
const high = parseInt(prompt("high"));
for (i = low; i <= high; i++) {
  let flag = 0;
  for (j = 2; j < 2; j++) {
    flag = 1;
    break;
  }
}
if (i > 1 && flag == 0) {
  console.log(i);
}
